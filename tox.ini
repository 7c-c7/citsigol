[tox]
envlist = py310, py311, py312, py313, lint, type

[testenv]
extras = dev
commands =
    pytest {posargs}

[testenv:lint]
skip_install = true
deps = ruff
commands =
    ruff check .
    ruff format --check .

[testenv:type]
extras = dev
commands =
    mypy .
